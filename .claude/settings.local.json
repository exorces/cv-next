{
  "permissions": {
    "allow": [
      "Bash(npx --no-install playwright --version)",
      "Bash(command -v npx)",
      "Bash(timeout 90 npx -y playwright@1.62.1 install --with-deps chromium)"
    ]
  }
}
